# Web Editor Monorepo (Scaffold)

本项目是一个 **pnpm + Turborepo** 的最小工作区示例，
包含：

- `packages/core` — TypeScript + React 的核心库（将来要发布到 npm）
- `apps/demo` — Vite + React 的演示应用，用于本地调试和演示组件

## 使用步骤

### 1. 安装依赖
```bash
pnpm install
